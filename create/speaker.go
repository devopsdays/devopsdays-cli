// Package create provides functions to create new content.
package create

import "github.com/devopsdays/devopsdays-cli/model"

// NewSpeaker takes in a constructed Speaker type and generates the stuff
func NewSpeaker(event model.Speaker, city string, year string) (err error) {

	return
}
